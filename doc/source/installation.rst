============
Installation
============

At the command line::

    $ pip install fuel-extension-volume-manager

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv fuel-extension-volume-manager
    $ pip install fuel-extension-volume-manager
